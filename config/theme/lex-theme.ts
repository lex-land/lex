// my-theme.ts
import { DefaultTheme } from 'styled-components';

const lexTheme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'rgba(0,0,0,0.45)',
  },
};

export { lexTheme };
